export const metadata = {
  title: "はじめまして",
  date: "2024-01-15",
  description: "新しいポートフォリオの最初の投稿です。",
  tags: ["meta", "nextjs"],
}

# はじめまして

新しいポートフォリオに最初の投稿です。**Next.js 16**・純粋な**Tailwind CSS**・ライブラリなしの自作i18nで構築しています。

## MDXが動く

ここでは通常のMarkdownとJSXコンポーネントが使えます。

```typescript
const greet = (name: string) => `こんにちは、${name}！`
console.log(greet('ユージーン'))
```

これからもっと投稿していきます。
